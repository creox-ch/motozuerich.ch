/* ============================================================
   MOTO-ZÜRICH — plan-colors.js
   Ручные цвета стендов для интерактивных планов.
   Один источник правды для Hallenplan (Halle 550) и StageOne.

   Структура:
     window.MZ_PLAN_COLORS = {
       "<page>": {                 // hallenplan | stageone
         "<Stand-ID>": {            // H01, AZ11, E17, S4 …
           "fill":   "#RRGGBB",     // заливка (кладётся полупрозрачно, план читаем)
           "border": "#RRGGBB",     // обводка/рамка
           "text":   "#RRGGBB"      // цвет подписи (там, где она есть)
         }
       }
     }
   Любое поле необязательно — задавай только то, что красишь.

   Как править:
   • Прямо руками в этом файле (пример ниже — раскомментируй/меняй).
   • Или через редактор на странице: открой план с ?colors в конце URL
     (или Shift+Alt+C), покрась стенды, нажми «Экспорт» и вставь
     полученный блок сюда взамен строки ниже.
   Значения из редактора (localStorage) всегда перекрывают этот файл,
   пока их не сбросишь — так что после «Экспорт → вставить сюда»
   можно очистить браузер, и карта останется той же.
   ============================================================ */

window.MZ_PLAN_COLORS = {
  "hallenplan": {
    // "H12": { "fill": "#FAF143", "border": "#e8a400" },
    // "H08": { "fill": "#C10D0D", "border": "#960909", "text": "#ffffff" }
  },
  "stageone": {
    // "E17": { "fill": "#549EF0", "border": "#2F65A0" }
  }
};
